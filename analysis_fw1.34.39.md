# Анализ прошивки Realtek RTL9210B v1.34.39.032625

## Содержимое архива

Архив `realtek_rtl9210B_fw1.34.39(station-drivers.com).zip` (18 MB, 38 файлов):

```
Windows/
├── RTL9210B_v1.34.39.032625.bin     ← Основная прошивка (целевая)
├── RTL9210B_v1.34.29.011325.bin     ← Предыдущая версия (для отката)
├── RTL9210B_v1.33.5.100623_signed.bin
├── RTL9210B_v1.32.87.082923_signed.bin
├── RTL9210B_v1.32.901.120722.bin
├── RTL9210B_gd_v4.30.23.071922.bin  ← Golden firmware (загрузчик/recovery)
├── UTHSB_MPtool_Lite.exe            ← Утилита прошивки (15 MB)
├── UTHSB_MPtool_Lite.ini            ← Конфигурация утилиты
├── configure/                        ← 13 cfg-файлов для разных чипсетов
│   ├── RTL9210B_CG.cfg             ← Основной конфиг RTL9210B
│   ├── RTL9210B_CG_182A.cfg        ← Расширенный конфиг
│   ├── RTL9210B_CG_Lite.cfg        ← Lite-конфиг
│   └── ... (AM8180, RTL9200/9201/9210/9210C/9211DS/9220*)
├── gdfw/                            ← Golden firmware для всех чипсетов
└── Mac/
    └── UTHSB_MPtool_Lite_Mini_v1.15.9.30314.dmg  ← Mac-утилита
```

## Анализ бинарного файла прошивки v1.34.39

| Параметр | Значение |
|---|---|
| Файл | `RTL9210B_v1.34.39.032625.bin` |
| Размер | 409 600 байт (400 KB) |
| SHA256 | `93fe6b1768bdf2de077aa68d9dc8142cbac7c8f8d6e5c48cacf5b573fabfdaef` |
| MD5 | `b5aa66157d6cacaeb6e4f4d0827473f8` |
| Header magic | `0x97463136` |
| Фактический размер кода | ~323.3 KB |
| Padding (0xFF) | ~76.7 KB |
| Содержит RSA-маркеры | Да (подписана) |

### Структура заголовка

```
Offset 0x00: 0x97463136  — Magic / идентификатор
Offset 0x04: 0x00000000  — Reserved
Offset 0x08: 0x0000AC03  — Общий для всех версий (маркер формата)
Offset 0x10: 0xBC3CC4AD  — Контрольная сумма / CRC (уникальна для каждой версии)
Offset 0x14: 0x00004000  — Offset загрузки (16 KB)
Offset 0x18: 0x00060000  — Размер кода (384 KB)
Offset 0x34: 0x00005A5A  — Маркер (одинаков для всех FW)
Offset 0x38: 0x00001D5A  — Маркер (одинаков для всех FW)
Offset 0x3C: 0x0000000E  — Флаги версии (v1.34.x = 0x0E, v1.32/1.33 = 0x00)
```

### Изменения относительно v1.34.29

| Метрика | Значение |
|---|---|
| Различающиеся регионы (merged) | 43 |
| Общий объём изменений | ~290 KB из 400 KB |
| Затронутые 4K-блоки | 78 из 100 |
| Прирост кода | +688 байт |

Несмотря на то, что прошивка выглядит как инкрементальное обновление (v1.34.29 → v1.34.39), изменения затрагивают ~78% 4K-блоков. Это характерно для Realtek firmware, где перекомпиляция приводит к сдвигам по всему образу.

### Найденные строки в прошивке

Строки указывают на поддерживаемые функции:

- **Ошибки PCIe**: `gen3 auto_cal error` — калибровка PCIe Gen3
- **SCSI/USB**: `Incorrect sector index %x %x` — обработка секторов
- **Файловые системы**: `Apple_HFS`, `Windows_NTFS`, `DOS_FAT_32`
- **SMART / Disk Info**: `Temperature`, `Data read`, `Data write`, `Power on hours`, `Power cycles`, `Unsafe off`
- **Разметка**: `Partition(%d/%d)`, `Partition-%d`
- **Скорость**: `linkspeed`
- **LCD-дисплей**: Присутствуют ASCII-art паттерны (поддержка LCD-экранов)
- **USB BOT**: `USBCUSBS` — USB Bulk-Only Transport маркеры
- **Версия**: `1.00RTL9210`, `Realtek USB CD-ROM 2.00`

### Энтропия по блокам

```
0x00000-0x03FFF: Заголовок + конфигурация (entropy 1.58 — структурированные данные)
0x04000-0x08FFF: Код инициализации / таблицы строк (entropy 4.0-5.5)
0x09000-0x2EFFF: Основной код прошивки (entropy 6.2-7.2 — плотный код)
0x2F000-0x34FFF: Таблицы данных / конфигурации (entropy 4.6-4.8)
0x35000-0x50FFF: Дополнительный код + RSA-подпись (entropy 6.1-7.1)
0x51000-0x63FFF: Padding 0xFF
```

Высокая энтропия (7.0+) в некоторых блоках может указывать на наличие RSA-подписи или сжатых данных.

## Golden Firmware (gd)

| Параметр | Значение |
|---|---|
| Файл | `RTL9210B_gd_v4.30.23.071922.bin` |
| Размер | 69 632 байт (68 KB) |
| Magic | `0x81798179` (отличается от основной FW) |
| Назначение | Recovery/bootstrap загрузчик |

Golden firmware — это минимальный загрузчик, записываемый в отдельную область флеш-памяти. Используется для восстановления при повреждении основной прошивки.

## Конфигурация утилиты (UTHSB_MPtool_Lite.ini)

```ini
[General]
TsVerion=2
MenuUpdate=1283
MenuPG=262400
MenuView=14
MenuDump=0
MenuOthers=0
CfgSel=configure/RTL9210B_CG.cfg          ← конфиг по умолчанию
FwFlashBinSel=RTL9210B_v1.34.29.011325.bin ← ⚠️ Указывает на v1.34.29, НЕ на v1.34.39!
FwGoldenBinSel=gdfw/RTL9210B_gd_v4.30.23.071922.bin
DiskPartition=GPT
DiskFormat=exFAT
DiskVolLabel=My USB
```

**Важно**: В ini-файле `FwFlashBinSel` указывает на `v1.34.29`, а не на `v1.34.39`. Перед прошивкой нужно вручную сменить на `RTL9210B_v1.34.39.032625.bin`.

## Конфигурация RTL9210B_CG.cfg (из архива)

```
VID = 0xbda
PID = 0x9210
MANUFACTURE = "Realtek"
SCSI_VENDOR = "Realtek"
PRODUCT = "RTL9210B-CG"
SCSI_PRODUCT = "RTL9210B-CG"
SERIAL = "012345678904"
;DISK_IPS_THRES = 10        ← закомментировано (рекомендуется)
;LED = 0x1                  ← закомментировано
CUSTOMIZED_LED = 01 01
;SUSPEND_LED_OFF = 0x0
UART_DBG_PIN = 5
```

## Сравнение с вашим конфигом

Ваш текущий конфиг:

```
VID = 0xbda
PID = 0x9210
MANUFACTURE = "Realtek"
SCSI_VENDOR = "Realtek"
PRODUCT = "RTL9210"              ← ⚠️ Отличие
SCSI_PRODUCT = "RTL9210 NVME"   ← ⚠️ Отличие
SERIAL = "012345679990"          ← Свой серийник (ОК)
LED = 0x2                        ← ⚠️ Отличие (0x2 вместо 0x1)
UART_DBG_PIN = 0x5
DISK_IPS_THRES : 0x0a = 0x5     ← ⚠️ Нестандартный синтаксис
```

### Таблица различий

| Параметр | Ваш конфиг | RTL9210B_CG.cfg (архив) | Комментарий |
|---|---|---|---|
| PRODUCT | `"RTL9210"` | `"RTL9210B-CG"` | У вас стоит имя для RTL9210(A), а не RTL9210B |
| SCSI_PRODUCT | `"RTL9210 NVME"` | `"RTL9210B-CG"` | Аналогично |
| SERIAL | `"012345679990"` | `"012345678904"` | Ваш — OK, оставляйте свой |
| LED | `0x2` | закомментировано | 0x2 — нестандартное значение; в архиве используется CUSTOMIZED_LED |
| CUSTOMIZED_LED | отсутствует | `01 01` | В архиве есть, у вас нет |
| DISK_IPS_THRES | `: 0x0a = 0x5` | закомментировано (`;`) | Синтаксис `: ... =` — это формат дампа, не конфига! |

### Проблема с DISK_IPS_THRES

Строка `DISK_IPS_THRES : 0x0a = 0x5` — это синтаксис из файла дампа (`dump`), а не из конфига (`cfg`). В cfg-файлах используется формат `DISK_IPS_THRES = 0x5`. Если утилита не распознаёт этот синтаксис, параметр может игнорироваться.

## Рекомендации для обновления

### 1. Подготовка конфига для прошивки v1.34.39

Рекомендуемый конфиг (на основе вашего дампа + лучшие практики):

```cfg
VID = 0xbda
PID = 0x9210
MANUFACTURE = "Realtek"
SCSI_VENDOR = "Realtek"
PRODUCT = "RTL9210B-CG"
SCSI_PRODUCT = "RTL9210B-CG"
SERIAL = "012345679990"
;DISK_IPS_THRES = 10
LED = 0x2
;CUSTOMIZED_LED = 01 01
;SUSPEND_LED_OFF = 0x0
UART_DBG_PIN = 5
```

Изменения:
- **PRODUCT** и **SCSI_PRODUCT** обновлены на `RTL9210B-CG` (для корректной идентификации)
- **SERIAL** — оставлен ваш
- **DISK_IPS_THRES** — закомментирован (рекомендуется для предотвращения случайных отключений диска)
- **LED = 0x2** — оставлен, если у вас конкретная модель с таким поведением LED
- Убран некорректный синтаксис из дампа

### 2. Процедура прошивки

1. **Сделайте дамп** текущей конфигурации через UTHSB_MPtool (кнопка Dump)
2. В `UTHSB_MPtool_Lite.ini` измените:
   ```
   FwFlashBinSel=RTL9210B_v1.34.39.032625.bin
   ```
3. Выберите подготовленный cfg-файл
4. Извлеките NVMe/SATA диск из корпуса перед прошивкой
5. Прошейте

### 3. Что нового в v1.34.39

Из changelog репозитория:
- **v1.34.29** (04.01.2025): Исправлена проблема со случайными отключениями SSD
- **v1.34.39** (13.01.2025): Дата сборки 26.03.2025, changelog пока не описан подробно

Прирост кода +688 байт и 78% изменённых блоков относительно v1.34.29 указывают на значительные внутренние изменения (возможно, дополнительные исправления стабильности).

### 4. Риски

- Прошивка подписана RSA — невозможно модифицировать бинарник
- При неудачной прошивке — процедура разблокировки через замыкание CS#/VCC на SPI-флеш
- Golden firmware не меняется между версиями (v4.30.23) — recovery всегда доступен
